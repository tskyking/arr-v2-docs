# ARR-V2 overnight QA — 2026-04-27 04:00 America/Los_Angeles

Scope: reviewed `docs/saas/arr-rebuild/nightly/`, especially the 03:00 Coordinator handoff and local dashboard-view changes, then re-ran safe deployability/frontend/backend validation. No external push attempted.

## Executive summary

- DigitalOcean deployability is materially improved versus 01:00: backend `build`/`start` scripts exist, production build emits `dist/server.js`, `PORT`/`API_PREFIX=/api` startup works, frontend root-hosted build configuration works, and draft DO App Platform files exist under `apps/arr-v2/deploy/`.
- Frontend/dashboard readiness is good locally: `npm run typecheck` passes, a root/API-path production build passes, and the dashboard-view HTML now reflects the overnight deployability state and open blockers.
- Backend production deploy smoke is green: `npm run build` passes and `API_PREFIX=/api PORT=19081 DATA_DIR=/tmp/arr-v2-qa-0400-data node dist/server.js` responds at `/api/health` with `{"status":"ok"}`.
- Backend CI readiness is still not green: `npm run typecheck` fails on test-only TypeScript issues, and `npm test` still fails because workbook fixtures are hardcoded to `/Users/sky/...` plus skipped/customer suites inherit that missing fixture condition.
- DigitalOcean spec is a useful draft but not apply-ready: GitHub repo placeholders must be replaced and a durable `DATA_DIR` persistence/database plan is still required before customer production use.
- Publishing remains blocked by GitHub HTTPS auth on this Mac; all findings/changes remain local.

## Commands run

From `/Users/tylerking/.openclaw/workspace/arr-v2-docs-work`:

```bash
cd apps/arr-v2/frontend && npm run typecheck
cd apps/arr-v2/frontend && VITE_BASE_PATH=/ VITE_API_BASE_PATH=/api npm run build
cd apps/arr-v2/backend && npm run build
cd apps/arr-v2/backend && npm run typecheck
cd apps/arr-v2/backend && npm test
cd apps/arr-v2/backend && API_PREFIX=/api PORT=19081 DATA_DIR=/tmp/arr-v2-qa-0400-data node dist/server.js
curl -fsS http://127.0.0.1:19081/api/health
```

## Results

| Area | Check | Result | Notes |
| --- | --- | --- | --- |
| Frontend | `npm run typecheck` | PASS | No TS errors. |
| Frontend | `VITE_BASE_PATH=/ VITE_API_BASE_PATH=/api npm run build` | PASS with warning | Build output succeeded; Vite warns that JS chunk is >500 kB (`662.06 kB`, gzip `186.50 kB`). |
| Backend | `npm run build` | PASS | `tsc -p tsconfig.build.json` plus esbuild bundle completed; `dist/server.js` emitted. |
| Backend | Start/health smoke | PASS | `API_PREFIX=/api`, `PORT=19081`, temp `DATA_DIR`; `/api/health` returned status ok. |
| Backend | `npm run typecheck` | FAIL | Test-only errors remain in `arrMovements.test.ts` old tenant signatures and `store-roundtrip.test.ts` bad `../../arr/src/types.js` import. |
| Backend | `npm test` | FAIL | 33/38 test files passed; 710/756 tests passed; 5 files failed, 34 tests failed, 12 skipped. Failures are concentrated around missing workbook fixture paths and customer-route fixture setup. |
| Docs/dashboard | Review of `dashboard-view/index.html` | PASS local | Dashboard reflects deployability cleanup, current validation, DO/persistence/auth blockers, and overnight coordination notes. |
| DigitalOcean | `apps/arr-v2/deploy/*` review | DRAFT only | Spec uses placeholder repo values and file-backed persistence; do not apply as-is. |

## Backend failure details

`npm run typecheck` failures:

- `services/api/src/__tests__/arrMovements.test.ts`: multiple `TS2554` failures because tests call tenant-aware functions with old single-argument signatures.
- `services/api/src/__tests__/store-roundtrip.test.ts(30,34)`: `TS2307` cannot find `../../arr/src/types.js`.

`npm test` failures:

- Summary: 5 failed files, 33 passed files, 34 failed tests, 710 passed tests, 12 skipped.
- Workbook fixture failures continue to reference hardcoded `/Users/sky/.openclaw/workspace/docs/saas/arr-rebuild/reference/source-examples/csv/...` paths.
- `xlsxXmlReader.test.ts`, `pipeline.integration.test.ts`, and real-upload portions of `server-upload.test.ts` fail because the workbook files are not found from this workspace path.
- `session10-customer-routes.test.ts` and `session11-customer-cube-routes.test.ts` are affected by import setup returning `422 FILE_NOT_FOUND` instead of loading the real workbook.

## DigitalOcean deployability assessment

Ready for prototype smoke once GitHub/account values are filled:

- Backend has reproducible install/build/start shape for App Platform.
- Backend honors `PORT`; route prefix smoke confirms `/api/health` works under `API_PREFIX=/api`.
- Frontend can build root-hosted assets with API calls routed to `/api`.
- Draft App Platform spec expresses two components: Node API service and static frontend.

Not ready for production/customer deployment:

- `digitalocean-app.yaml` still has `REPLACE_WITH_GITHUB_OWNER_REPO` placeholders.
- `DATA_DIR=/workspace/data` is file-backed and likely ephemeral unless a persistent storage design is attached; choose managed DB/object storage/persistent disk before production.
- Backend typecheck/test gates are red, so CI should not be treated as green.
- GitHub HTTPS auth is blocked, so DigitalOcean deploy-on-push cannot see these local-only changes yet.

## Recommended next owner actions

1. Fix backend test portability first: replace `/Users/sky/...` fixture constants with repo-relative discovery or committed test-fixture paths.
2. Update `arrMovements.test.ts` to use the current tenant-aware API signatures.
3. Fix the `store-roundtrip.test.ts` type import path.
4. Decide production persistence for `DATA_DIR` before any customer-facing DO deployment.
5. Replace DO spec repo placeholders and validate in a real DO project only after GitHub auth/publishing is restored.
