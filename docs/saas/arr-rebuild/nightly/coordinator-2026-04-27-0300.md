# ARR-V2 overnight Coordinator — 2026-04-27 03:00 America/Los_Angeles

Scope: reviewed the 01:00 QA and 02:00 Dev notes, refreshed the public dashboard-view HTML locally, and prepared QA-ready coordination guidance. No external push attempted because the standing GitHub HTTPS authentication blocker remains documented in nightly status.

## QA-ready summary

- ARR-V2 is closer to DigitalOcean deployability than it was at 01:00: backend build/start/test/typecheck scripts and explicit dev metadata now exist, production sources bundle to `dist/server.js`, and `API_PREFIX=/api` was verified with a local health check.
- Frontend deployment flexibility improved: `VITE_BASE_PATH=/` supports root-hosted static output while preserving the existing GitHub Pages default, and API routing can be configured through `VITE_API_BASE_URL` or `VITE_API_BASE_PATH`.
- Draft deploy handoff material now exists under `apps/arr-v2/deploy/`, including a DigitalOcean App Platform spec that intentionally contains placeholder GitHub repo/account values.
- Validation is mixed but actionable: frontend typecheck and demo/root builds pass, backend production build/start passes, but full backend typecheck still fails on pre-existing test-only issues.
- Backend CI/test cleanup is now the main technical blocker: update old tenant-aware `arrMovements` test signatures, fix the `store-roundtrip` relative type import, and replace hardcoded `/Users/sky` workbook fixture paths with repo-relative resolution.
- Production readiness still requires a persistence decision: current backend storage is file-backed under `DATA_DIR`, which is acceptable for prototype validation but not safe for customer production use without durable storage/database planning.
- Publishing remains blocked locally by unauthenticated GitHub HTTPS, so dashboard/deployability changes are left in the working tree for the main session or for a later authenticated push.

## Dashboard update

Updated `docs/saas/arr-rebuild/dashboard-view/index.html` locally to reflect the overnight QA/Dev cycle:

- phase changed to deployability cleanup plus product signoff;
- overall progress note now reflects current 2026-04-27 validation instead of older April 3 green-test language;
- next actions now prioritize backend test portability, DigitalOcean spec/persistence validation, and GitHub auth/publishing;
- added a visible overnight coordination card with QA/Dev/current validation/open blocker notes;
- milestone labels now show repo/package hygiene in progress and staging/hosting drafted.

## Recommended next owner actions

1. Assign Dev to backend test cleanup until `npm run typecheck` and `npm test` are at least locally meaningful from a clean install.
2. Assign QA to re-run backend clean-install verification after fixture/test fixes, including a real workbook path-portability check.
3. Have product/ops decide whether the first DigitalOcean target uses managed DB/object storage, persistent disk, or remains prototype-only with explicit data-loss warnings.
4. Restore or provide GitHub auth before attempting to publish dashboard-view or deploy docs remotely.
