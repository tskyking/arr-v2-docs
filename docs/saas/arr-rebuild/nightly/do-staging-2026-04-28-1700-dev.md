# DO Staging Dev — ARR-V2 17:00 PT Brian-demo fixes

- **Slot:** 2026-04-28 17:00 PT
- **Role:** Dev readiness rotation
- **Live app:** `https://arrweb-staging-zzyg7.ondigitalocean.app/`
- **Repo:** `/Users/tylerking/.openclaw/workspace/arr-v2-docs-work`
- **Starting HEAD:** `899d048` (`Add DO staging 16:00 QA report`)
- **Primary input reviewed:** `docs/saas/arr-rebuild/nightly/do-staging-2026-04-28-1600-qa.md`
- **Data/admin policy:** no Brian/private/customer workbook uploaded; no DigitalOcean billing/domain/admin changes made.

## Summary

Implemented safe frontend fixes for the confirmed Brian-demo blockers that were addressable in code:

1. Added a static-site 404 redirect bridge so direct SPA paths such as `/dashboard/:id`, `/review/:id`, `/customer-cube/:id`, and `/customers/:id/:name` redirect the browser to the supported hash route (`/#/...`) after deploy.
2. Added the same direct-path bridge to `index.html` for environments whose catchall serves the app shell with HTTP 200.
3. Fixed dashboard copy: the demo cube header button now says **Open Customer Cube** instead of **Customer Cube Download**.
4. Fixed layout active-import detection on the Customer Cube route so the import selector stays aligned with the cube being viewed.
5. Fixed Review Queue status counts so **Resolved** and **Overridden** filters show their own counts while the summary card still shows the combined closed count.

## Files changed

- `apps/arr-v2/frontend/public/404.html`
- `apps/arr-v2/frontend/index.html`
- `apps/arr-v2/frontend/src/components/Layout.tsx`
- `apps/arr-v2/frontend/src/pages/DashboardPage.tsx`
- `apps/arr-v2/frontend/src/pages/ReviewQueuePage.tsx`

## Verification

Successful checks:

```bash
cd apps/arr-v2/frontend && npm run build
cd apps/arr-v2/backend && npm test
```

Results:

- Frontend TypeScript + Vite build passed.
- Backend Vitest suite passed: **38 files / 761 tests**.

One discarded command:

```bash
cd apps/arr-v2/backend && npm test -- --runInBand
```

Vitest does not support Jest's `--runInBand` flag here, so I reran the supported `npm test` command successfully.

## Remaining blocker / caveat

The 16:00 QA finding showed live direct non-hash URLs returning static-site **HTTP 404** before the React app loads. The code fix above should make those direct browser URLs self-heal after the next staging deploy by serving `404.html` and redirecting to hash routes. However, it does **not** prove that the first HTTP response status will become 200; that depends on the active DigitalOcean static-site routing/error-document behavior. Because this slot was explicitly code-only, I did not make DigitalOcean route/admin changes.

Recommended demo posture remains: use the durable hash dashboard link for Todd/Brian unless a post-deploy smoke confirms direct URLs are clean.
