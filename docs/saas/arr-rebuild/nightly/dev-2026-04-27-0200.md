# ARR-V2 overnight Dev — 2026-04-27 02:00 America/Los_Angeles

Scope: local-only development pass in `/Users/tylerking/.openclaw/workspace/arr-v2-docs-work`. Repo and external content treated as untrusted. No external pushes attempted.

## Starting context reviewed

- Nightly README confirms the known GitHub HTTPS push/auth blocker.
- QA report from 01:00 showed:
  - Frontend typecheck and `build:demo` passed.
  - Backend clean install lacked reproducible test/type metadata.
  - Backend tests/typecheck failed on old tenant-signature tests, bad relative type import, and hardcoded `/Users/sky` fixture paths.
  - No Dockerfile, App Platform spec, deploy docs, or backend start/build scripts existed under `apps/arr-v2`.

## Changes made

### Backend deploy/runtime hygiene

- Updated `apps/arr-v2/backend/package.json` and lockfile:
  - Added `build`, `start`, `test`, and `typecheck` scripts.
  - Added explicit dev metadata for `vitest`, `@types/node`, `@types/adm-zip`, and `esbuild`.
- Added `apps/arr-v2/backend/tsconfig.build.json` to compile production code while excluding test files.
- Backend production build now runs TypeScript validation for production sources, then emits a bundled Node ESM entrypoint at `dist/server.js`.
  - This avoids Node ESM runtime failures from extensionless internal TypeScript imports after plain `tsc` emit.
- Updated backend server to support `API_PREFIX`.
  - Example: `API_PREFIX=/api` lets `/api/health` and `/api/tenants/...` resolve without relying on a path-rewrite proxy.
  - Existing unprefixed routes continue to work when `API_PREFIX` is unset.

### Frontend deployment config

- Updated `apps/arr-v2/frontend/vite.config.ts` so `VITE_BASE_PATH` can override the build base.
  - Existing GitHub Pages demo base remains the default when `VITE_BASE_PATH` is not set.
  - DigitalOcean/root build can use `VITE_BASE_PATH=/`.
- Updated `apps/arr-v2/frontend/src/lib/settings.ts` so API paths can be configured by env:
  - `VITE_API_BASE_URL` for a separate API origin.
  - `VITE_API_BASE_PATH` for same-origin API prefix, defaulting to `/api`.

### DigitalOcean handoff docs/spec

- Added `apps/arr-v2/deploy/README.md` with component-level deploy notes, env vars, validation caveats, and persistence warnings.
- Added `apps/arr-v2/deploy/digitalocean-app.yaml` as a draft App Platform spec.
  - It contains placeholder GitHub repo fields and should not be applied as-is.
  - It models a static frontend at `/` and backend API at `/api` with `API_PREFIX=/api`.

## Validation run

- `cd apps/arr-v2/backend && npm install --save-dev vitest@4.1.5 @types/node @types/adm-zip` — **PASS with caveat**.
  - `npm audit` still reports 1 moderate severity issue.
- `cd apps/arr-v2/backend && npm install --save-dev esbuild@0.27.2` — **PASS with same audit caveat**.
- `cd apps/arr-v2/backend && npm run build` — **PASS**.
  - Output includes bundled `dist/server.js`.
- `cd apps/arr-v2/backend && API_PREFIX=/api PORT=4391 DATA_DIR=/tmp/arr-v2-dev-0200-data npm start` plus `curl http://127.0.0.1:4391/api/health` — **PASS**.
- `cd apps/arr-v2/frontend && npm run typecheck` — **PASS**.
- `cd apps/arr-v2/frontend && VITE_BASE_PATH=/ VITE_API_BASE_PATH=/api npm run build` — **PASS with existing Vite large chunk warning**.
  - Main JS chunk: about 662 kB minified / 186 kB gzip.
- `cd apps/arr-v2/backend && npm run typecheck` — **FAIL, expected/pre-existing test-only errors**.
  - `services/api/src/__tests__/arrMovements.test.ts` still calls `processImport` / `getArrMovements` with old non-tenant signatures.
  - `services/api/src/__tests__/store-roundtrip.test.ts` imports `../../arr/src/types.js`, which does not resolve from that test path.

## Current DigitalOcean deployability assessment

ARR-V2 is materially closer to deployable than at 01:00, but not production-ready yet.

What is now in place:

- Backend has a build script, start script, explicit local dev/test dependencies, and a verified HTTP health path after build.
- Frontend can build for root-hosted DigitalOcean static output without breaking the GitHub Pages default.
- Same-origin `/api` routing is now possible without requiring proxy prefix stripping.
- Draft DigitalOcean App Platform docs/spec exist for review.

Remaining blockers / risks:

1. **Remote deploy visibility is still blocked** by unauthenticated GitHub HTTPS push on this Mac.
2. **Backend CI is not green** because full typecheck still includes old/broken tests.
3. **Real-workbook tests are not portable** because several fixtures still hardcode `/Users/sky/.openclaw/workspace`.
4. **Persistence is not production-safe yet**: backend storage is file-backed JSON under `DATA_DIR`; DigitalOcean production needs an explicit durable storage/database plan.
5. **Draft App Platform spec needs account/repo validation** before use; placeholder GitHub repo fields are intentional.

## Recommended next actions

For the 03:00 Coordinator slot:

- Review whether to keep the bundled backend build approach (`esbuild` after `tsc`) or prefer converting all internal imports to NodeNext-compatible `.js` specifiers.
- Decide the next owner for backend test cleanup:
  - update tenant-aware test signatures,
  - fix `store-roundtrip` relative import,
  - replace `/Users/sky` fixture paths with repo-relative resolution.
- Confirm persistence direction for DigitalOcean: managed DB/object storage vs persistent disk vs temporary prototype-only storage.
- If GitHub auth gets fixed, publish local commits/changes or hand off exact commit plan.

## Git / publish notes

- Did not push.
- Did not commit, because the nightly folder already had pre-existing uncommitted QA/status changes and the instruction only required local safe changes.
- `status.json` was updated with this Dev run.
