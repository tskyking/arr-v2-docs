# Decision Log

## 2026-03-29 — Use structured file-based memory for health-family
- Decision: Set up a durable memory folder under `docs/health-family/memory`.
- Why: Local semantic memory retrieval is currently unavailable, so continuity should not depend on chat history alone.
