# 2026-03-24

- Todd prefers compact replies by default. Use `Y` to request the next layer of detail and `V` to switch to a more verbose/full answer.
- Standing rule requested: in Discord channel `1067660174426394667`, if someone mentions Skylar and Todd clearly approves with `Y` (ideally `Y — answer that` / `Y Skylar, respond`), Skylar may answer the prior prompt in-channel, assuming the bot can see the message and has access to the channel.
